DATABASE_URL="postgresql://peerdeliver:peerdeliver_dev@localhost:5432/peerdeliver"
JWT_SECRET="change-me-to-a-random-secret"
JWT_REFRESH_SECRET="change-me-to-another-random-secret"
PORT=3001
NODE_ENV=development

# Stripe — optional in dev; payment endpoints will 503 if unset.
# Test keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""
# Get from `stripe listen --forward-to localhost:3001/webhooks/stripe`
STRIPE_WEBHOOK_SECRET=""
STRIPE_PLATFORM_COUNTRY="CH"
PLATFORM_FEE_PERCENT=10
